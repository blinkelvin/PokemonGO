# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PokemonGO' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PokemonGO
	pod 'Alamofire', '~> 4.4'
  	pod 'Kingfisher', '~> 3.0'

  target 'PokemonGOTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PokemonGOUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
